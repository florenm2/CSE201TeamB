These three standalone .java files read a .csv file and create Course objects. We can use this base code and build
around it using the code already given to us.

Note: You must change the String file in ImportCSV.java

-Adam